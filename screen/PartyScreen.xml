<?xml version="1.0" encoding="UTF-8"?>
<screen xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns=""
        xsi:noNamespaceSchemaLocation="http://moqui.org/xsd/xml-screen-2.1.xsd"
        require-authentication="anonymous-all">

    <transition name="createParty">
        <service-call name="create#com.party.Party"/>
        <actions>
            <if condition="partyTypeEnumId == 'PERSON'">
                <set field="partyForm" value="/apps/person"/>
                <else-if condition="partyTypeEnumId == 'PARTY_GROUP'">
                    <set field="partyForm" value="/apps/partyGroup"/>
                    <else>
                        <set field="partyForm" value="."/>
                    </else>
                </else-if>
            </if>
        </actions>
        <default-response url="${partyForm}"/>
    </transition>

    <actions>
        <!--Entity finds for the screen Party entity-->
        <entity-find entity-name="com.party.Party" list="partyList"><search-form-inputs/></entity-find>
    </actions>
    <widgets>
<!--        Container for the screen Party entity-->
        <container-dialog id="CreatePartyDialog" button-text="Create Party">
            <form-single name="CreateParty" transition="createParty">

                <auto-fields-entity entity-name="com.party.Party" field-type="edit"/>

                <field name="partyTypeEnumId">
                    <default-field title="Party Type">
                        <drop-down>
                           <list-options list="partyList" key="${partyTypeEnumId}"/>
                        </drop-down>
                    </default-field>
                </field>

                <field name="submit">
                    <default-field title="Submit"><submit/></default-field>
                </field>
            </form-single>

        </container-dialog>

        <container>
            <label text="Party List" type="h1"/>
            <form-list name="Party" list="partyList">
                <field name="partyId">
                    <header-field title="Party ID" show-order-by="true"/>
                    <default-field><display/></default-field>
                </field>
                <field name="partyTypeEnumId">
                    <header-field title="Party Type Enum Id" show-order-by="true"/>
                    <default-field><display/></default-field>
                </field>
                <field name="createDate">
                    <header-field title="Create Date" show-order-by="true"/>
                    <default-field><display/></default-field>
                </field>
            </form-list>
        </container>
    </widgets>
</screen>